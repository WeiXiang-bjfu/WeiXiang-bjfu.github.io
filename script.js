const root=document.documentElement;const themeButton=document.getElementById('themeButton');const menuButton=document.querySelector('.menu-toggle');const nav=document.getElementById('mainnav');function applyTheme(theme){root.dataset.theme=theme;localStorage.setItem('wx-theme',theme);themeButton.textContent=theme==='dark'?'☀':'◐';themeButton.title=theme==='dark'?'Switch to light theme':'Switch to dark theme'}const savedTheme=localStorage.getItem('wx-theme');const prefersDark=window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches;applyTheme(savedTheme||(prefersDark?'dark':'light'));themeButton.addEventListener('click',()=>{applyTheme(root.dataset.theme==='dark'?'light':'dark')});menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));const pubSearch=document.getElementById('pubSearch');const pubs=[...document.querySelectorAll('.pub-item')];const filterButtons=[...document.querySelectorAll('.filter')];const noResults=document.getElementById('noResults');let activeFilter='all';function filterPublications(){const query=(pubSearch.value||'').toLowerCase().trim();let visible=0;pubs.forEach(pub=>{const text=pub.textContent.toLowerCase();const tags=(pub.dataset.tags||'').toLowerCase();const matchesText=!query||text.includes(query)||tags.includes(query);const matchesTag=activeFilter==='all'||tags.split(/\s+/).includes(activeFilter);const show=matchesText&&matchesTag;pub.hidden=!show;if(show)visible++});noResults.hidden=visible!==0}pubSearch.addEventListener('input',filterPublications);filterButtons.forEach(button=>{button.addEventListener('click',()=>{filterButtons.forEach(b=>b.classList.remove('active'));button.classList.add('active');activeFilter=button.dataset.filter;filterPublications()})});document.getElementById('year').textContent=new Date().getFullYear();